import{notFound}from"next/navigation";import{CampaignDetail}from"@/components/matatu/matatu-ui";import{matatuContext}from"@/lib/services/matatu-data";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{id:string}>}){const x=await matatuContext(),id=(await params).id,c=x.campaigns.find(v=>v.id===id);if(!c)notFound();return <CampaignDetail campaign={c} participation={x.participations.find(p=>p.campaign_id===id)}/>}
