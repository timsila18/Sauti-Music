import {notFound} from "next/navigation";import {SongPrivate} from "@/components/artist/artist-ui";import {artistContext} from "@/lib/services/artist-data";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{id:string}>}){const {songs,campaigns}=await artistContext(),id=(await params).id;const song=songs.find(x=>x.id===id);if(!song)notFound();return <SongPrivate song={song} campaigns={campaigns.filter(x=>x.song_id===song.id)}/>}
