import {notFound} from "next/navigation";import {ArtistShell,Header} from "@/components/artist/artist-ui";import {artistContext} from "@/lib/services/artist-data";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{id:string}>}){const x=await artistContext(),id=(await params).id,c=x.campaigns.find(v=>v.id===id);if(!c)notFound();return <ArtistShell><Header eyebrow="Campaign Results" title={c.campaign_name}/><div className="mt-8 grid gap-4 sm:grid-cols-2"><section className="rounded-3xl bg-card p-6"><h2 className="text-xl font-medium">Campaign Summary</h2><p className="mt-4 text-muted-foreground">5 qualified plays · 1 DJ · 1 matatu · Nairobi and CBD – Umoja</p></section><section className="rounded-3xl bg-card p-6"><h2 className="text-xl font-medium">Engagement</h2><p className="mt-4 text-muted-foreground">327 saves · listener likes and requests will accumulate here.</p></section></div><p className="mt-5 text-sm text-muted-foreground">Structured for a future export; PDF export is not active yet.</p></ArtistShell>}
