import Link from "next/link";import {ArtistShell,Header} from "@/components/artist/artist-ui";import {Button} from "@/components/ui/button";import {artistContext} from "@/lib/services/artist-data";export const dynamic="force-dynamic";export default async function Page(){const {account}=await artistContext();return <ArtistShell><Header eyebrow={account.account_type.replaceAll("_"," ")} title="Public Profile"/><div className="mt-8 rounded-3xl bg-card p-7"><h2 className="text-3xl font-medium text-plum">{account.name}</h2><p className="mt-2 text-muted-foreground">@{account.handle} · {account.town_area}, {account.county}</p><p className="mt-5 max-w-xl">{account.bio}</p><Button asChild className="mt-6 rounded-full"><Link href={`/artist/${account.handle}`}>View Public Profile</Link></Button></div><p className="mt-5 text-sm text-muted-foreground">Profile editing controls will retain handle validation and public-safe fields only.</p></ArtistShell>}
