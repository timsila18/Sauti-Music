import {notFound} from "next/navigation";import {AppShell} from "@/components/app-shell";import {PublicProfileView} from "@/components/listener/listener-ui";import {requireRole} from "@/lib/auth/session";import {profileByHandle} from "@/lib/services/listener-discovery";
export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{handle:string}>}){await requireRole("LISTENER","/discover");const profile=profileByHandle((await params).handle);if(!profile||profile.kind!=="ARTIST")notFound();return <AppShell role="listener" action={{role:"listener",label:"Start Listening",supportingText:"Discover what's playing around you."}}><PublicProfileView profile={profile}/></AppShell>}
