import{notFound}from"next/navigation";import{SetView}from"@/components/dj/dj-ui";import{djContext}from"@/lib/services/dj-data";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{id:string}>}){const x=await djContext(),id=(await params).id,s=x.sets.find(v=>v.id===id);if(!s)notFound();return <SetView set={s} campaigns={x.campaigns.filter(c=>x.participations.some(p=>p.campaign_id===c.id&&["ACCEPTED","ACTIVE"].includes(p.status)))}/>}
