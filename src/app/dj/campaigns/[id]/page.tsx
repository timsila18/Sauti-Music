import{notFound}from"next/navigation";import{Campaign}from"@/components/dj/dj-ui";import{djContext}from"@/lib/services/dj-data";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{id:string}>}){const x=await djContext(),id=(await params).id,c=x.campaigns.find(v=>v.id===id);if(!c)notFound();return <Campaign campaign={c} part={x.participations.find(p=>p.campaign_id===id)}/>}
