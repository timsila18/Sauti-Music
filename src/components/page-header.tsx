export function PageHeader({ eyebrow, title, description }: { eyebrow: string; title: string; description: string }) { return <header className="mb-10"><p className="text-sm font-medium text-coral">{eyebrow}</p><h1 className="mt-2 text-4xl font-medium tracking-[-.04em] text-plum sm:text-5xl">{title}</h1><p className="mt-3 max-w-xl text-muted-foreground">{description}</p></header>; }
export function SectionHeader({ title, action }: { title: string; action?: string }) { return <div className="mb-5 flex items-end justify-between gap-4"><h2 className="text-2xl font-medium tracking-tight text-plum">{title}</h2>{action ? <button className="text-sm font-medium text-coral hover:underline">{action}</button> : null}</div>; }
